[build]
dockerfilePath = "alfred/Dockerfile"

[deploy]
startCommand = "alembic upgrade head && uvicorn alfred.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
